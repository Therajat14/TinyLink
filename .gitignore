# Root-level node_modules and build artifacts
node_modules/
dist/
build/
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS-specific files
.DS_Store
Thumbs.db

# IDE-specific files
.vscode/
.idea/
*.swp

# Optional dependency directories
.client-node_modules/
.server-node_modules/

# React build
client/build/

# Server-only
server/node_modules/
server/.env
server/dist/

# Optional: if using testing
coverage/
*.lcov

# Optional: if using Docker
docker-compose.override.yml

# Optional: if using eslint or prettier with caching
.eslintcache
.prettiercache

# Optional: if using next.js (just in case)
.next/
out/

# Optional: if using SASS
.sass-cache/

# Optional: if using Firebase
.firebase/
.firebaserc

# Optional: if using TypeScript
*.tsbuildinfo
